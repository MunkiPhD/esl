<div id="user-login" class="right">
  <% if user_signed_in? %>
    <i class="icon-user"></i>&nbsp;<%= link_to current_user.username, user_preferences_path %>
    ( <%= link_to 'logout', destroy_user_session_path, :method => :delete %> )
  <% else %>
    <%= link_to 'login', new_user_session_path %>
  <% end %>
</div>

