<h1>Workouts#index</h1>

<div id="workouts">
  <% @workouts.each do |workout| %>
    <div>
      <h4><%= link_to workout.title, workout %></h4>
    </div>
  <% end %>
</div>

<%= link_to "new workout", new_workout_path %>
