<h1>My Workouts</h1>

<div id="workouts">
  <ul>
  <% @workouts.each do |workout| %>
    <li><%= link_to workout.title, workout %> <%= "(#{days_ago workout.date_performed})" %></li>
  <% end %>
</ul>
</div>

<%= link_to "new workout", new_workout_path %>
